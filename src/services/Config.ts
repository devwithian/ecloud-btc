export const STALE_PRICE_THRESHOLD = 60 * 2; // 2 minutes, to be calculated in seconds with date operation

export const RESOLUTION_TIME = 60; // 60 seconds, to be calculated in seconds with date operation
export const RESOLUTION_TIME_BUFFER = 5; // Add 5 seconds buffer after the 60 seconds period

export const POLLING_PRICE_INTERVAL_MS = 10000; // 10 seconds, unit in milliseconds
export const POLLING_GUESS_INTERVAL_MS = 15000; // 15 seconds, unit in milliseconds
